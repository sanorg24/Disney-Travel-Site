// Shared site-wide constants used by the sitemap, robots.txt, canonical
// tags, and Open Graph/Twitter metadata. Single source of truth for the
// production domain so it only needs to be updated in one place if it
// ever changes (currently matches the repository's CNAME file).
export default {
  siteUrl: "https://www.pixiepackedfamily.com",
  defaultSocialImage: "/photos/brand-banner.jpg",
};
